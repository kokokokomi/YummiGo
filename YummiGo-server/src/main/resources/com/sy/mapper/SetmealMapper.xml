<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sy.mapper.SetmealMapper">

    <resultMap id="BaseResultMap" type="com.sy.entity.Setmeal">
            <id property="id" column="id" />
            <result property="categoryId" column="category_id" />
            <result property="name" column="name" />
            <result property="price" column="price" />
            <result property="status" column="status" />
            <result property="description" column="description" />
            <result property="image" column="image" />
            <result property="isDeleted" column="is_deleted" />
            <result property="version" column="version" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="createUser" column="create_user" />
            <result property="updateUser" column="update_user" />
    </resultMap>

    <sql id="Base_Column_List">
        id,category_id,name,price,status,description,
        image,is_deleted,version,create_time,update_time,
        create_user,update_user
    </sql>
    <select id="pageQuerySetmealVO" resultType="com.sy.vo.SetmealVO">
        SELECT s.*,c.name AS categoryName
        FROM setmeal s
        LEFT JOIN category c
        ON s.category_id=c.id
        <where>
            <if test="name!=null">
                AND s.name LIKE CONCAT('%', #{dto.name}, '%')
            </if>
            <if test="status@=null">
                AND s.status=#{dto.status}
            </if>
            <if test="categoryId != null">
                and s.category_id = #{dto.categoryId}
            </if>
        </where>
        ORDER BY s.create_time desc
    </select>
</mapper>
