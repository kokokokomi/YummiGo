<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sy.mapper.DishMapper">

    <resultMap id="BaseResultMap" type="com.sy.entity.Dish">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="categoryId" column="category_id" />
            <result property="price" column="price" />
            <result property="image" column="image" />
            <result property="description" column="description" />
            <result property="status" column="status" />
            <result property="isDeleted" column="is_deleted" />
            <result property="version" column="version" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="createUser" column="create_user" />
            <result property="updateUser" column="update_user" />
    </resultMap>

    <sql id="Base_Column_List">
        id,name,category_id,price,image,description,
        status,is_deleted,version,create_time,update_time,
        create_user,update_user
    </sql>
    <select id="selectDishVOPage" resultType="com.sy.vo.DishVO">
        SELECT
        d.id,
        d.name,
        d.category_id,
        d.price,
        d.image,
        d.description,
        d.status,
        d.update_time,
        c.name AS categoryName
        FROM
        dish d
        LEFT JOIN
        category c ON d.category_id = c.id
        WHERE
        d.is_deleted = 0
        <if test="dto.name != null and dto.name != ''">
            AND d.name LIKE CONCAT('%', #{dto.name}, '%')
        </if>
        <if test="dto.categoryId != null">
            AND d.category_id = #{dto.categoryId}
        </if>
        <if test="dto.status != null">
            AND d.status = #{dto.status}
        </if>
        ORDER BY
        d.update_time DESC
    </select>
    <select id="getBySetmealId" resultType="com.sy.entity.Dish">
        SELECT *
        FROM dish
        WHERE id IN (
            SELECT dish_id
            FROM setmeal_dish
            WHERE setmeal_id = #{id}
        )
    </select>
    <select id="selectDishWithFlavorByCategory" resultType="com.sy.vo.DishVO">
        SELECT
            d.*,
            f.*
        FROM dish d
                 LEFT JOIN dish_flavor f ON d.id = f.dish_id AND f.is_deleted = 0
        WHERE d.category_id = #{categoryId}
          AND d.status = 1
          AND d.is_deleted = 0
    </select>
</mapper>
